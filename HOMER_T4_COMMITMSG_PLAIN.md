# T4 commit-message mode: deterministic (Plain)

This file is committed with the DETERMINISTIC (Plain) commit-message mode
(CommitMessageMode.deterministic), which skips the AI summariser entirely and
uses the fixed PRD §2.3 template built by build_deterministic_message().
